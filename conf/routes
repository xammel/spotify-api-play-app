# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                                   controllers.HomeController.index()
GET     /artist                             controllers.ApiCallController.getArtist(artistId)
GET     /search                             controllers.ApiCallController.findArtist(accessToken, query)
GET     /authorize                          controllers.AuthorizationController.authorize()
GET     /authorization-callback             controllers.AuthorizationController.callback(code)
GET     /home                               controllers.HomeController.home()
GET     /my-top-artists                     controllers.ApiCallController.getMyTopArtists()
GET     /my-top-tracks                      controllers.ApiCallController.getMyTopTracks()
GET     /recommendations                    controllers.ApiCallController.getRecommendedTracks()
GET     /save-track                         controllers.ApiCallController.saveTrack(trackId : String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
